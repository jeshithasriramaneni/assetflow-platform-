const {PrismaClient}=require('@prisma/client');const prisma=new PrismaClient();async function fix(){const assets=await prisma.asset.findMany({where:{qrCode:null}});console.log('Assets without QR:',assets.length);for(const a of assets){const qrCode='ASSET-'+Date.now()+'-'+Math.random().toString(36).substring(2,9).toUpperCase();await prisma.asset.update({where:{id:a.id},data:{qrCode}});console.log('Fixed:',a.name)}await prisma.$disconnect();}fix(); 
